/*
function log(msg) {
  console.log(msg);
}

let msg = "Hello World";
log(msg);
*/

/*
for (let i = 0; i < 5; i++) {
  console.log(i);
}

console.log(i) // ts tells us i is not available in this scope
*/

// in terminal, run "tsc file.ts" to compile ts code into js (a new .js file is created)
// var is used instead of let/const with variable declaration